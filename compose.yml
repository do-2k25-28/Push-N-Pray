services:
  postgres:
    image: library/postgres:18.3
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DATABASE: pushnpray
    volumes:
      - postgres:/var/lib/postgresql
    ports:
      - 5432:5432

volumes:
  postgres:
